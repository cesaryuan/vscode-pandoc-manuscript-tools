{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Pandoc Crossref Helper",
      "type": "extensionHost",
      "request": "launch",
      "preLaunchTask": "bundle",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/test_project",
        "--goto",
        "${workspaceFolder}/test_project/README.md:1:1"
      ]
    }
  ]
}
